'''
Use the `print()` function to accept one or more arguments.
The argument(s) are written to the standard output (stdout).
'''

print("Hello in Python")

'''
Default `print()` behaviors:
- Accepts one or more arguments which can be of various data types.
- Before printing, each argument is converted to a string representation.
- If multiple arguments are provided, space characters are used as seperators.
- A newline character is added after writing the argument(s).
'''
